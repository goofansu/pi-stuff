# AGENTS.md

## Code style

- When creating or modifying a pi extension, work in the `pi/extensions/` directory.
- Extensions are TypeScript files that follow the pi coding agent extension API.
- The `pi/` directory contents may be symlinked to `~/.pi/agent/` for active use.
